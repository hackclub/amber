<div class="max-w-sm mx-auto mt-20 text-center">
  <h1 class="text-2xl font-bold mb-2">Sign in failed</h1>
  <p class="text-gray-600 mb-6"><%= message&.humanize.presence || "Something went wrong signing you in." %></p>
  <%= link_to "Back to tickets", root_path, class: "text-red-600 underline" %>
</div>
